from typing import Any

# dict/dictionary purposs- A dictionary stores data in pairs
user_data: dict[str, Any]  = [
    {'name': 'juan', 'age': 22},
    {'name': 'maria', 'age': 25},
    {'name': 'pedro', 'age': 30}]

# retrieve = user_data.get('name')
# retrieve = user_data.items('name')

    user_data[][
        if 
    ] = 

def updateInfo():
    updateName = str(input("update Name: "))
    updateAge = str(input("update Age: "))


inputName = str(input("Enter username: "))
    if inputName in user_data['name']
        return updateInfo()
    else
        exit